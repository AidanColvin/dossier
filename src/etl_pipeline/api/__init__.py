"""http api layer.

a thin fastapi service over the pipeline. importing this subpackage requires
the optional `api` dependencies (fastapi, uvicorn); the core pipeline itself
stays standard-library only.
"""
