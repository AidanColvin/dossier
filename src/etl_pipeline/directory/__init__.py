"""company directory: every sec-listed company as a searchable table.

built from the sec's own exchange-annotated tickers file rather than a
hand-curated list, so it is always as current as edgar and costs nothing
to maintain. search, filter, sort, and csv export all happen server-side
in python.
"""
